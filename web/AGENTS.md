# web 目录说明

`web/` 是 Human Infra 的 Astro + D3 Web 应用目录，用于把科研叙事、书籍转译、预测模型和可视化组织成可发布的多页站点。

## 目录结构

```text
web/
├── AGENTS.md                    # 本目录架构说明
├── README.md                    # 运行方式、页面职责和边界
├── astro.config.mjs             # Astro 配置
├── package.json                 # Web 应用依赖与脚本
├── public/                       # 静态发布资源和 vendored 前端资源
├── scripts/                      # 本地生成器和数据导出脚本
└── src/
    ├── components/              # Astro 页面组件
    ├── data/                    # 小型结构化展示数据和派生审计产物
    ├── layouts/                 # 页面布局
    ├── pages/                   # Astro 路由页面
    │   └── papers/              # 独立论文页路由
    ├── scripts/                 # D3 图表脚本
    └── styles/                  # 全局视觉系统
```

## 职责边界

- `src/pages/*.astro` 是页面真相源，负责叙事结构、页面路由和内容组织。
- `src/pages/papers/effective-immortality-flywheel.astro` 是有效永生飞轮的独立论文页，复用 PaperReaderLayout，不覆盖旧 `/paper/`。
- `src/pages/papers/metric-redshift-recursive-waiting.astro` 是度规红移递归等待假设的独立论文页，复用 PaperReaderLayout，不覆盖旧 `/paper/` 或有效永生飞轮论文页。
- `src/pages/papers/proper-time-differential-waiting-hypothesis.astro` 是度规红移固有时差分路径的专项论文页，由 arXiv HTML Paper Reuse Kit 生成 seed 后维护正文，消费 `src/data/proper-time-differential-waiting-hypothesis.json`，复用 PaperReaderLayout，不覆盖其他论文页。
- `src/pages/papers/controllable-metric-waiting-room-hypothesis.astro` 是可控度规等待室假设的专项论文页，由 `tools/arxiv_html_paper_tool.py write-page` 新建 seed 后维护正文，消费 `src/data/controllable-metric-waiting-room-hypothesis.json`，用于承载“等待室 -> 技术升级 -> 退出采用 -> 再等待”的递归未来等待假设。
- `src/scripts/evidence-graph.js` 从页面嵌入的 Evidence Matrix JSON 生成 D3 证据链 SVG，只负责可视化主链、反哺路径、风险通道和因果门禁，不发起网络请求。
- `src/scripts/model-charts.js` 保存演示计算和 D3 图表逻辑，不保存页面长文。
- `src/data/effective-immortality-evidence.json` 是有效永生飞轮论文页的结构化研究数据源；Research Question Brief、Methodology Blueprint、Research Execution Roadmap、Search Strategy Seeds、Systematic Search Protocol、Literature Screening Rubric、Literature Gap Map、Search Execution Register、Candidate Source Verification Register、Candidate Source Extraction Register、Claim Register、Claim Evidence Map、AI Research Failure Mode Audit、AI Task Evidence Register、Reference Registry、变量字典、模型契约、推导图、链路边、技术族和证据等级应先更新这里，再由页面消费。
- `src/data/metric-redshift-recursive-waiting.json` 是度规红移递归等待假设论文页的结构化研究数据源；Problem Anchor、Paper Plan、贡献注册表、Claim Register、Claim-Evidence Matrix、Claim Maturity Register、Falsifier Register、Source Cards、Source Card Ledger、Scenario Card Template、Scenario Evaluation Registry、Toy NCG Evaluation Protocol、Toy NCG Evaluation Registry、NCG Formula Decomposition、Pre-Submission Review Register、变量契约、模型假设、命题注册表、生命周期状态机、图表计划、引用语境预审、研究协议和失败条件应先更新这里，再由页面消费。
- `src/data/proper-time-differential-waiting-hypothesis.json` 是度规红移固有时差分路径专项论文页的结构化研究数据源；概念表、核心假设、贡献注册表、生命周期状态机、NCG 变量、可达性阶梯、反证条件、Source Cards、Claim-Evidence Matrix、Scenario Cards、Scenario Evaluation Registry、Qualitative NCG Scale、Scenario Variable Matrix、Toy NCG Evaluation Protocol、NCG Formula Decomposition、数据驱动 SVG 场景比较图、图表计划、引用边界、Citation Context Review Packet、Reference Registry 和页面审计契约应先更新这里，再由页面消费。
- `src/data/controllable-metric-waiting-room-hypothesis.json` 是可控度规等待室论文页的结构化研究数据源；概念、核心假设、Research Questions、形式化假设、公式、命题、Claim-Evidence、Citation Context、状态机、递归等待链路、变量契约、场景比较、失败条件、研究协议、Evidence Needs、Evaluation Roadmap、Pre-Submission Review、来源和页面审计契约应先更新这里，再由页面消费。
- `src/data/metric-redshift-recursive-waiting/` 保存红移等待论文的工具生成 seed、citation context packet、citation context local review、scenario evaluation audit 和 coverage audit 账本，当前包括 `TOOL_GENERATED_PAGE_SEED.astro`、`CITATION_CONTEXT_REVIEW_PACKET.json`、`CITATION_CONTEXT_REVIEW_PACKET.md`、`CITATION_CONTEXT_LOCAL_REVIEW.json`、`CITATION_CONTEXT_LOCAL_REVIEW.md`、`SCENARIO_EVALUATION_AUDIT.json`、`SCENARIO_EVALUATION_AUDIT.md`、`COVERAGE_AUDIT.json` 和 `COVERAGE_AUDIT.md`，除工具 seed 外不得手工编辑。
- `src/data/proper-time-differential-waiting-hypothesis/` 保存度规红移固有时差分路径专项论文的工具生成 seed、reference verification、citation context packet、citation context local review、pre-submission review 和 coverage audit 账本，当前包括 `TOOL_GENERATED_PAGE_SEED.astro`、`REFERENCE_VERIFICATION.json`、`REFERENCE_VERIFICATION.md`、`CITATION_CONTEXT_REVIEW_PACKET.json`、`CITATION_CONTEXT_REVIEW_PACKET.md`、`CITATION_CONTEXT_LOCAL_REVIEW.json`、`CITATION_CONTEXT_LOCAL_REVIEW.md`、`PRE_SUBMISSION_REVIEW.json`、`PRE_SUBMISSION_REVIEW.md`、`COVERAGE_AUDIT.json` 和 `COVERAGE_AUDIT.md`；seed 用于证明页面由 arXiv HTML Paper Reuse Kit 创建，不作为正文真相源，正文真相源是同级 JSON 文件 `src/data/proper-time-differential-waiting-hypothesis.json`。
- `src/data/controllable-metric-waiting-room-hypothesis/` 保存可控度规等待室论文的工具生成 seed、reference verification、citation context packet、citation context local review、pre-submission review、公式推导包和覆盖审计账本，当前包括 `TOOL_GENERATED_PAGE_SEED.astro`、`REFERENCE_VERIFICATION.json`、`REFERENCE_VERIFICATION.md`、`CITATION_CONTEXT_REVIEW_PACKET.json`、`CITATION_CONTEXT_REVIEW_PACKET.md`、`CITATION_CONTEXT_LOCAL_REVIEW.json`、`CITATION_CONTEXT_LOCAL_REVIEW.md`、`PRE_SUBMISSION_REVIEW.json`、`PRE_SUBMISSION_REVIEW.md`、`DERIVATION_PACKAGE.md`、`COVERAGE_AUDIT.json` 和 `COVERAGE_AUDIT.md`；seed 必须保持工具原样，引用审查账本不得伪装成外部审稿结论，推导包必须符合 formula-derivation 结构，覆盖审计由 `npm run audit:controllable-metric-waiting-room` 生成。
- `src/data/effective-immortality-flywheel/` 保存从 Reference Registry、citation-context review packet、citation-context risk triage、high-risk citation context review、citation-context local review、fresh reviewer citation audit protocol、fresh reviewer context packets、fresh reviewer handoff、fresh reviewer citation results assembly、fresh reviewer citation results audit、primary-source verification、claim-context alignment audit、coverage audit 和 pre-submission review 生成的 `references.bib`、`CITATION_AUDIT.json`、`CITATION_AUDIT.md`、`CITATION_CONTEXT_REVIEW_PACKET.json`、`CITATION_CONTEXT_REVIEW_PACKET.md`、`CITATION_CONTEXT_RISK_TRIAGE.json`、`CITATION_CONTEXT_RISK_TRIAGE.md`、`HIGH_RISK_CITATION_CONTEXT_REVIEW.json`、`HIGH_RISK_CITATION_CONTEXT_REVIEW.md`、`CITATION_CONTEXT_LOCAL_REVIEW.json`、`CITATION_CONTEXT_LOCAL_REVIEW.md`、`FRESH_REVIEWER_CITATION_AUDIT_PROTOCOL.json`、`FRESH_REVIEWER_CITATION_AUDIT_PROTOCOL.md`、`fresh-reviewer-context-packets/`、`FRESH_REVIEWER_CITATION_AUDIT_RESULTS_AUDIT.json`、`FRESH_REVIEWER_CITATION_AUDIT_RESULTS_AUDIT.md`、`FRESH_REVIEWER_CITATION_AUDIT_RESULTS_TEMPLATE.json`、`FRESH_REVIEWER_CITATION_AUDIT_RESULTS_TEMPLATE.md`、`CLAIM_CONTEXT_AUDIT.json`、`CLAIM_CONTEXT_AUDIT.md`、`REFERENCE_VERIFICATION.json`、`REFERENCE_VERIFICATION.md`、`COVERAGE_AUDIT.json`、`COVERAGE_AUDIT.md`、`PRE_SUBMISSION_REVIEW.json` 和 `PRE_SUBMISSION_REVIEW.md`，不得手工编辑，使用 `npm run export:references`、`npm run export:citation-contexts`、`npm run audit:citation-risk`、`npm run audit:high-risk-citations`、`npm run audit:citation-contexts-local`、`npm run export:fresh-reviewer-protocol`、`npm run export:fresh-reviewer-context-packets`、`npm run export:fresh-reviewer-handoff`、`npm run assemble:fresh-reviewer-results`、`npm run audit:fresh-reviewer-results`、`npm run verify:references`、`npm run audit:claim-context`、`npm run audit:effective-immortality`、`npm run audit:pre-submission` 或 `npm run audit:paper-integrity` 重新生成。正式 `FRESH_REVIEWER_CITATION_AUDIT_RESULTS.json` 只能由 assembler 在外部逐项 `results/CTX*.json` 齐全后生成。
- `scripts/export-reference-registry.mjs` 是本地引用产物生成器，读取 `src/data/effective-immortality-evidence.json`，输出 BibTeX 和 citation-audit 账本。
- `scripts/verify-reference-registry.mjs` 是本地联网参考文献核验器，读取 `src/data/effective-immortality-evidence.json`，用 DOI/Crossref、arXiv API、官方 URL 和本地 artifact 输出 primary-source verification 账本。
- `scripts/audit-claim-context-alignment.mjs` 是本地 claim-context 对齐审计器，读取核心主张、claim evidence map、citation context register、Reference Registry 和 Reference Verification，输出 claim-context audit 账本。
- `scripts/audit-effective-immortality-coverage.mjs` 是本地覆盖审计器，读取结构化证据数据和 Source Cards，检查 source alias、AI task reference key、AI task full-text verification、method-audit 必填字段、research declarations、estimand registry、analysis plan registry 和 AI research failure mode audit，输出 coverage audit 账本。
- `scripts/evaluate-metric-redshift-scenarios.mjs` 是本地红移等待 scenario evaluator，读取 Scenario Evaluation Registry 与 Toy NCG Evaluation Registry，输出 scenario evaluation audit 账本。
- `scripts/export-metric-redshift-citation-context-review-packet.mjs` 是本地红移等待 citation context 外部审查输入包生成器，读取红移等待 Claim Register、Claim Evidence Map、Citation Context Register 和 Source Cards，输出 fresh reviewer 可消费的引用语境审查输入包。
- `scripts/export-proper-time-differential-citation-context-review-packet.mjs` 是本地度规红移固有时差分路径 citation context 外部审查输入包生成器，读取 Source Cards、Claim-Evidence Matrix、Citation Context Rows 和 Reference Registry，输出 fresh reviewer 可消费的引用语境审查输入包。
- `scripts/audit-proper-time-differential-citation-context-local-review.mjs` 是本地度规红移固有时差分路径引用语境边界审查器，读取 citation context packet，输出 safe use、forbidden use、risk tier 和 fresh reviewer focus 账本；它不替代外部逐项 reviewer verdict。
- `scripts/verify-proper-time-differential-references.mjs` 是本地度规红移固有时差分路径参考来源可达性核验器，读取 Reference Registry，使用 curl 和本地路由检查输出 reference verification 账本；它证明来源入口可达，不证明来源支持具体主张。
- `scripts/audit-proper-time-differential-pre-submission-review.mjs` 是本地度规红移固有时差分路径预提交审查器，读取结构化数据、页面源码、reference verification、citation context packet 和 citation context local review，输出提交边界、非预测器边界、外部审查边界、文风和阻塞项账本；它不替代正式同行评议。
- `scripts/audit-metric-redshift-citation-context-local-review.mjs` 是本地红移等待引用语境边界审查器，读取 citation context packet，输出本地 safe use、forbidden use、risk tier 和 fresh reviewer focus 账本；它不替代外部逐项 reviewer verdict。
- `scripts/audit-metric-redshift-paper.mjs` 是本地红移等待论文覆盖审计器，读取红移等待结构化数据、citation context packet、citation context local review、scenario evaluation audit 和论文页，检查主张、来源、变量、章节和页面必需字符串是否对齐，输出 coverage audit 账本。
- `scripts/audit-proper-time-differential-paper.mjs` 是本地度规红移固有时差分路径论文覆盖审计器，读取结构化 JSON 数据源、专项论文页、工具 seed、reference verification、citation context packet 和 citation context local review，检查数据集合最低行数、Source Card 引用闭合、Citation Context Rows 指向已登记主张和来源、引用语境本地审查计数闭合、参考来源核验无 FAIL、Scenario Evaluation 指向已登记场景、Scenario Variable Matrix 使用受控等级且 NCG 分类与场景评估一致、NCG Formula Block 指向已登记变量、F4 `scenario-ncg-matrix-svg` 场景比较图、15 个章节、TOC、页面数据消费、arXiv DOM 契约和重复 class 属性，输出 coverage audit 账本。
- `scripts/audit-controllable-metric-waiting-room-paper.mjs` 是本地可控度规等待室论文覆盖审计器，读取结构化 JSON 数据源、专项论文页、工具 seed、reference verification、citation context packet、citation context local review、pre-submission review 和公式推导包，检查数据集合最低行数、Research Questions、Evidence Needs、Evaluation Roadmap、F1/F2 图表、14 个章节、TOC、页面数据消费、推导包必需段落、arXiv DOM 契约和重复 class 属性，输出 coverage audit 账本。
- `scripts/verify-controllable-metric-waiting-room-references.mjs` 是本地可控度规等待室参考来源可达性核验器，读取 `sourceRows`，使用 curl 和本地路由检查输出 reference verification 账本；它证明来源入口可达，不证明来源支持具体主张。
- `scripts/export-controllable-metric-waiting-room-citation-context-review-packet.mjs` 是本地可控度规等待室 citation context 外部审查输入包生成器，读取 Claim-Evidence、Citation Context 和 Source Rows，输出 fresh reviewer 可消费的引用语境审查输入包。
- `scripts/audit-controllable-metric-waiting-room-citation-context-local-review.mjs` 是本地可控度规等待室引用语境边界审查器，读取 citation context packet，输出 safe use、forbidden use、risk tier 和 fresh reviewer focus 账本；它不替代外部逐项 reviewer verdict。
- `scripts/audit-controllable-metric-waiting-room-pre-submission-review.mjs` 是本地可控度规等待室预提交审查器，读取结构化数据、页面源码、reference verification、citation context packet 和 citation context local review，输出 working paper 边界、非预测器边界、外部审查边界、文风和阻塞项账本；它不替代正式同行评议。
- `scripts/audit-pre-submission-review.mjs` 是本地预提交审查器，读取论文、页面、source-notes、结构化数据和已生成 audit ledger，检查版本漂移、必需边界、机械文风问题和提交前阻塞项，输出 pre-submission review 账本。
- `scripts/export-citation-context-review-packet.mjs` 是本地外部审查输入包生成器，读取 Claim Register、Claim Evidence Map、Citation Context Register 和 Reference Registry，输出 fresh reviewer 可消费的 citation-context review packet。
- `scripts/audit-citation-context-risk-triage.mjs` 是本地 L3 引用语境风险分诊器，读取 citation-context review packet、Reference Verification 和 Claim Context Audit，输出外部 reviewer 的优先级队列；它不替代 fresh reviewer 结论。
- `scripts/audit-high-risk-citation-contexts.mjs` 是本地 HIGH/BLOCKER 引用语境来源边界审查器，读取 citation-context risk triage、Reference Verification 和结构化证据，只审最高风险上下文；它可以收窄提交前队列，但不替代 fresh reviewer 逐句判断。
- `scripts/audit-citation-context-local-review.mjs` 是本地全量引用语境来源边界审查器，读取 citation-context review packet、risk triage、Reference Verification、Claim Context Audit 和 high-risk review，覆盖 CTX1-CTX12；它固定 safe use、forbidden use、reference state 和 reviewer focus，但不替代 fresh reviewer 逐句判断。
- `scripts/export-fresh-reviewer-citation-audit-protocol.mjs` 是外部 fresh reviewer 审稿协议导出器，读取 citation-context packet、risk triage、local review、high-risk review、Reference Verification 和 Citation Audit，输出 reviewer independence contract、pass gate、review queue 和逐项 prompt；它不替代 reviewer verdict。
- `scripts/export-fresh-reviewer-context-packets.mjs` 是外部 fresh reviewer 分件包导出器，读取 fresh reviewer protocol，把 CTX1-CTX12 拆成独立 JSON/Markdown packet 和结果 stub，便于逐条外部审稿；它不替代 reviewer verdict。
- `scripts/export-fresh-reviewer-handoff.mjs` 是外部 fresh reviewer 单文件交接包生成器，读取 fresh reviewer protocol、packet index 和全部 CTX packet，输出 `fresh-reviewer-context-packets/HANDOFF.json`、`HANDOFF.md`、`RESULT_SCHEMA.json` 与 `RESULT_SCHEMA.md`；它把 independence contract、返回文件清单、verdict schema、阻塞条件、结果回填 schema、source trace 和完整 packet 内容合并成独立消费包，但不得生成 reviewer verdict。
- `scripts/audit-fresh-reviewer-handoff.mjs` 是外部 fresh reviewer handoff 审计器，验证 `HANDOFF.*`、`RESULT_SCHEMA.*`、`INDEX.*` 和各 CTX packet 的覆盖、schema 编译、source trace hash、结果文件清单和 Markdown 完整性，输出 `HANDOFF_AUDIT.json` 与 `HANDOFF_AUDIT.md`；它不替代 reviewer verdict。
- `scripts/assemble-fresh-reviewer-citation-results.mjs` 是外部 fresh reviewer 结果汇总器，读取 `fresh-reviewer-context-packets/results/CTX*.json`，先用 Ajv / `RESULT_SCHEMA.json` 做 JSON Schema 校验，再执行本地语义门禁；只有全部 12 个 context 齐全且结构校验通过后，才生成正式 `FRESH_REVIEWER_CITATION_AUDIT_RESULTS.json` 并写入每个输入文件的 SHA-256；它不得生成或推断 reviewer verdict。
- `scripts/audit-fresh-reviewer-citation-results.mjs` 是外部 fresh reviewer 结果账本审计器，生成 reviewer results 模板，若存在真实 `FRESH_REVIEWER_CITATION_AUDIT_RESULTS.json` 则验证它是否由 assembler 生成、是否能回到逐项 `results/CTX*.json` 输入文件、hash 是否匹配、每个 context 的 reviewer verdict、source URL、resolution status 和 blocking conditions；它不得生成伪 reviewer 结果。
- `src/layouts/PaperReaderLayout.astro` 直接复用 `public/static/browse/0.3.4/` 和 `public/use.typekit.net/` 中下载并归一化的 arXiv HTML papers CSS/JS/图标/字体资源；模板真相源沉淀在 `../tools/arxiv-html-paper/templates/`，不维护自定义论文阅读器样式或控件脚本。
- `src/components/` 保存可复用的 Astro 页面组件。
- `src/data/` 保存 Web 展示所需的小型结构化数据，不保存原始研究数据、医疗数据或个人数据。
- `web/dist/` 是构建产物，不进入版本库。
- 根目录 `singularity-human-infra.html` 保留为历史静态专项页；新的正式 Web 版本以 `web/` 为主。

## 维护规则

- 新增页面时，必须同步更新 `README.md` 和本文件目录结构。
- 新增图表时，优先复用 `src/scripts/model-charts.js` 和 `src/scripts/evidence-graph.js` 的 D3 绘图约定；证据型图表优先消费 `src/data/` 的结构化数据。
- 新增论文式长文时，优先使用 `tools/arxiv_html_paper_tool.py write-page` 生成工具 seed，再用 `ltx_*` 语义结构和 PaperReaderLayout 承载正文；不得改写 arXiv 下载资源的样式和控件逻辑，只替换正文内容。
- 修改 `src/data/proper-time-differential-waiting-hypothesis.json`、`src/pages/papers/proper-time-differential-waiting-hypothesis.astro`、`scripts/verify-proper-time-differential-references.mjs`、`scripts/export-proper-time-differential-citation-context-review-packet.mjs`、`scripts/audit-proper-time-differential-citation-context-local-review.mjs`、`scripts/audit-proper-time-differential-pre-submission-review.mjs` 或 `scripts/audit-proper-time-differential-paper.mjs` 时，必须运行 `npm run audit:proper-time-differential` 并提交生成后的 `REFERENCE_VERIFICATION.json`、`REFERENCE_VERIFICATION.md`、`CITATION_CONTEXT_REVIEW_PACKET.json`、`CITATION_CONTEXT_REVIEW_PACKET.md`、`CITATION_CONTEXT_LOCAL_REVIEW.json`、`CITATION_CONTEXT_LOCAL_REVIEW.md`、`PRE_SUBMISSION_REVIEW.json`、`PRE_SUBMISSION_REVIEW.md`、`COVERAGE_AUDIT.json` 与 `COVERAGE_AUDIT.md`；同时保留工具 seed、结构化 JSON 数据源、F4 `scenario-ncg-matrix-svg`、`ltx_page_navbar > nav.ltx_TOC`、`ltx_document`、`ltx_abstract` 和 `ltx_bibliography`。
- 修改 `src/data/controllable-metric-waiting-room-hypothesis.json`、`src/pages/papers/controllable-metric-waiting-room-hypothesis.astro`、`src/data/controllable-metric-waiting-room-hypothesis/DERIVATION_PACKAGE.md`、`scripts/verify-controllable-metric-waiting-room-references.mjs`、`scripts/export-controllable-metric-waiting-room-citation-context-review-packet.mjs`、`scripts/audit-controllable-metric-waiting-room-citation-context-local-review.mjs`、`scripts/audit-controllable-metric-waiting-room-pre-submission-review.mjs`、`scripts/audit-controllable-metric-waiting-room-paper.mjs` 或工具 seed `src/data/controllable-metric-waiting-room-hypothesis/TOOL_GENERATED_PAGE_SEED.astro` 时，必须运行 `npm run audit:controllable-metric-waiting-room` 并提交生成后的 `REFERENCE_VERIFICATION.json`、`REFERENCE_VERIFICATION.md`、`CITATION_CONTEXT_REVIEW_PACKET.json`、`CITATION_CONTEXT_REVIEW_PACKET.md`、`CITATION_CONTEXT_LOCAL_REVIEW.json`、`CITATION_CONTEXT_LOCAL_REVIEW.md`、`PRE_SUBMISSION_REVIEW.json`、`PRE_SUBMISSION_REVIEW.md`、`COVERAGE_AUDIT.json` 和 `COVERAGE_AUDIT.md`；同时保留工具 seed、结构化 JSON 数据源、推导包、F2 `controllable-metric-waiting-room-loop-svg`、`ltx_page_navbar > nav.ltx_TOC`、`ltx_document`、`ltx_abstract` 和 `ltx_bibliography`。
- 新增或修改有效永生飞轮证据时，先更新 `src/data/effective-immortality-evidence.json`，再同步 Markdown source-note 和页面叙事。
- `ajv` 与 `ajv-formats` 只用于本地 fresh reviewer result schema 验收，不得被当作前端运行时状态管理或浏览器交互依赖。
- 修改 Reference Registry 后，必须运行 `npm run export:references` 和 `npm run verify:references`，并提交生成后的 `references.bib`、`CITATION_AUDIT.json`、`CITATION_AUDIT.md`、`REFERENCE_VERIFICATION.json` 和 `REFERENCE_VERIFICATION.md`。
- 修改 `claimRegister`、`claimEvidenceMap`、`citationContextRegister`、`referenceRegistry` 或 `REFERENCE_VERIFICATION` 后，必须运行 `npm run audit:claim-context`，并提交生成后的 `CLAIM_CONTEXT_AUDIT.json` 和 `CLAIM_CONTEXT_AUDIT.md`。
- 修改 `claimRegister`、`claimEvidenceMap`、`citationContextRegister` 或 `referenceRegistry` 后，必须运行 `npm run export:citation-contexts`，并提交生成后的 `CITATION_CONTEXT_REVIEW_PACKET.json` 和 `CITATION_CONTEXT_REVIEW_PACKET.md`。
- 修改 `claimRegister`、`claimEvidenceMap`、`citationContextRegister`、`referenceRegistry`、`REFERENCE_VERIFICATION` 或 `CLAIM_CONTEXT_AUDIT` 后，必须运行 `npm run audit:citation-risk`，并提交生成后的 `CITATION_CONTEXT_RISK_TRIAGE.json` 和 `CITATION_CONTEXT_RISK_TRIAGE.md`。
- 修改 `citationContextRegister`、`referenceRegistry`、`REFERENCE_VERIFICATION` 或 `CITATION_CONTEXT_RISK_TRIAGE` 后，必须运行 `npm run audit:high-risk-citations`，并提交生成后的 `HIGH_RISK_CITATION_CONTEXT_REVIEW.json` 和 `HIGH_RISK_CITATION_CONTEXT_REVIEW.md`。
- 修改 `citationContextRegister`、`referenceRegistry`、`REFERENCE_VERIFICATION`、`CLAIM_CONTEXT_AUDIT`、`CITATION_CONTEXT_RISK_TRIAGE` 或 `HIGH_RISK_CITATION_CONTEXT_REVIEW` 后，必须运行 `npm run audit:citation-contexts-local`，并提交生成后的 `CITATION_CONTEXT_LOCAL_REVIEW.json` 和 `CITATION_CONTEXT_LOCAL_REVIEW.md`。
- 修改 `citationContextRegister`、`referenceRegistry`、`REFERENCE_VERIFICATION`、`CLAIM_CONTEXT_AUDIT`、`CITATION_CONTEXT_RISK_TRIAGE`、`HIGH_RISK_CITATION_CONTEXT_REVIEW` 或 `CITATION_CONTEXT_LOCAL_REVIEW` 后，必须运行 `npm run export:fresh-reviewer-protocol`，并提交生成后的 `FRESH_REVIEWER_CITATION_AUDIT_PROTOCOL.json` 和 `FRESH_REVIEWER_CITATION_AUDIT_PROTOCOL.md`。
- 修改 `FRESH_REVIEWER_CITATION_AUDIT_PROTOCOL` 后，必须运行 `npm run export:fresh-reviewer-context-packets`、`npm run export:fresh-reviewer-handoff` 和 `npm run audit:fresh-reviewer-handoff`，并提交生成后的 `fresh-reviewer-context-packets/INDEX.*`、各 CTX packet、`HANDOFF.*`、`RESULT_SCHEMA.*` 与 `HANDOFF_AUDIT.*`。
- 新增外部 fresh reviewer 逐项结果时，把 `CTX*.json` 放入 `fresh-reviewer-context-packets/results/` 并保留这些输入文件，先运行 `npm run assemble:fresh-reviewer-results`，再运行 `npm run audit:fresh-reviewer-results`；只有 assembler 写出且 hash 可回溯的 `FRESH_REVIEWER_CITATION_AUDIT_RESULTS.json` 可作为正式结果账本。
- 修改 `FRESH_REVIEWER_CITATION_AUDIT_PROTOCOL` 或新增真实 `FRESH_REVIEWER_CITATION_AUDIT_RESULTS.json` 后，必须运行 `npm run audit:fresh-reviewer-results`，并提交生成后的 results template 和 `FRESH_REVIEWER_CITATION_AUDIT_RESULTS_AUDIT.*`；禁止手工把 pending 状态改成 pass。
- 修改 Research Question Brief、Methodology Blueprint、Search Strategy Seeds、Systematic Search Protocol、Literature Screening Rubric、Literature Gap Map、Search Execution Register、Candidate Source Verification Register、Candidate Source Extraction Register、`evidenceEdges[*].sources`、`technologyFamilies[*].evidenceSources`、`aiTaskEvidenceRegister[*].referenceKey`、`estimandRegistry`、`analysisPlanRegistry`、`aiResearchFailureModeAudit`、Source Cards 或 Reference Registry 后，必须运行 `npm run audit:effective-immortality`，并提交生成后的 `COVERAGE_AUDIT.json` 和 `COVERAGE_AUDIT.md`。
- 修改 working paper、Web paper page、Evidence Matrix、derivation package、citation audit notes、版本号、schema version 或任一审计产物状态后，必须运行 `npm run audit:pre-submission`，并提交生成后的 `PRE_SUBMISSION_REVIEW.json` 和 `PRE_SUBMISSION_REVIEW.md`。
- 修改 `src/data/metric-redshift-recursive-waiting.json`、`src/pages/papers/metric-redshift-recursive-waiting.astro`、`scripts/evaluate-metric-redshift-scenarios.mjs` 或工具 seed `src/data/metric-redshift-recursive-waiting/TOOL_GENERATED_PAGE_SEED.astro` 后，必须运行 `npm run audit:metric-redshift`，并提交生成后的 `src/data/metric-redshift-recursive-waiting/SCENARIO_EVALUATION_AUDIT.json`、`SCENARIO_EVALUATION_AUDIT.md`、`COVERAGE_AUDIT.json` 和 `COVERAGE_AUDIT.md`。
- 修改红移等待 `citationContextRegister`、`sourceCards`、`claimEvidenceMap`、`scripts/export-metric-redshift-citation-context-review-packet.mjs` 或 `scripts/audit-metric-redshift-citation-context-local-review.mjs` 后，必须运行 `npm run audit:metric-redshift`，并提交生成后的 `CITATION_CONTEXT_REVIEW_PACKET.*`、`CITATION_CONTEXT_LOCAL_REVIEW.*`、`SCENARIO_EVALUATION_AUDIT.*` 和 `COVERAGE_AUDIT.*`。
- 重新安装或校验 arXiv HTML papers 资源时，使用 `python3 tools/arxiv_html_paper_tool.py verify-assets --public-dir web/public` 或 `scaffold`，不要手工拼资源路径。
- 页面中的预测模型必须保留用途边界：研究展示、变量解释、群体级路径位移，不输出个体死亡日期。
- 任何从书籍、论文或网页引入的观点必须标明它是学习资料、转译、假设还是研究标准。
- 引入新的前端依赖前，先确认 Astro / MDX / D3 是否已经足够；复杂状态管理再评估 Svelte islands 或 React islands。
